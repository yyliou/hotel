% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_panel.R, R/twhotel.R
\name{htp_build_panel}
\alias{htp_build_panel}
\title{Build the full hotel-by-month panel}
\usage{
htp_build_panel(
  start_ym = NULL,
  end_ym = NULL,
  out_csv = "tourist_hotel_panel.csv",
  cache_dir = "htp_cache",
  formats = "XLSX",
  reports = NULL,
  overwrite = FALSE,
  throttle = 0.5,
  verbose = TRUE
)

htp_build_panel(
  start_ym = NULL,
  end_ym = NULL,
  out_csv = "tourist_hotel_panel.csv",
  cache_dir = "htp_cache",
  formats = "XLSX",
  reports = NULL,
  overwrite = FALSE,
  throttle = 0.5,
  verbose = TRUE
)
}
\arguments{
\item{start_ym, end_ym}{Inclusive period bounds as `YYYYMM` integers
(e.g. `202301`, `202512`). `NULL` leaves that side unbounded.}

\item{out_csv}{Path to write the panel (UTF-8 with BOM). `NULL` to skip.}

\item{cache_dir}{Directory for downloaded source files.}

\item{formats}{File format to download/parse (default `"XLSX"`).}

\item{reports}{Optional pre-fetched [htp_list_reports()] tibble.}

\item{overwrite}{Re-download cached files.}

\item{throttle}{Seconds between network requests.}

\item{verbose}{Progress messages.}

\item{include_cumulative}{Also include the cumulative range files
(e.g. "202401-12"). Default FALSE to avoid double-counting months.}

\item{colmap}{Column map passed to \code{htp_parse_report}.}
}
\value{
A tibble: the combined panel (also written to \code{out_csv}).

A tibble: the combined panel (also written to `out_csv`).
}
\description{
End-to-end pipeline: list reports -> filter by date range -> download ->
parse -> bind into one panel -> (optionally) write a UTF-8-BOM CSV.

End-to-end pipeline: list reports, filter by date, download, parse, bind into
one panel, and (optionally) write a UTF-8 (BOM) CSV. Only single-month files
are used (cumulative range files are skipped to avoid double counting).
}
\examples{
\dontrun{
panel <- htp_build_panel(202301, 202512, out_csv = "tourist_hotel_panel.csv")
}
}
