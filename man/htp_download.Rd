% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R, R/twhotel.R
\name{htp_download}
\alias{htp_download}
\title{Download a report file by its AttFile id}
\usage{
htp_download(
  file_id,
  dest_dir = "htp_cache",
  ext = "xlsx",
  overwrite = FALSE,
  throttle = 0.5
)

htp_download(
  file_id,
  dest_dir = "htp_cache",
  ext = "xlsx",
  overwrite = FALSE,
  throttle = 0.5
)
}
\arguments{
\item{file_id}{The numeric AttFile id (from [htp_list_reports()]).}

\item{dest_dir}{Cache directory (created if needed).}

\item{ext}{File extension for the saved file (default `"xlsx"`).}

\item{overwrite}{Re-download even if a cached copy exists.}

\item{throttle}{Seconds to pause before the request.}
}
\value{
The local file path (invisibly on cache hit).

The local file path (invisibly on a cache hit).
}
\description{
Download a report file by its AttFile id

Download one report file by its AttFile id
}
\examples{
\dontrun{
p <- htp_download(htp_list_reports()$file_id[1])
}
}
