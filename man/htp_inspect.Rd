% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R, R/twhotel.R
\name{htp_inspect}
\alias{htp_inspect}
\title{Inspect the raw layout of a downloaded report}
\usage{
htp_inspect(path, n = 25)

htp_inspect(path, n = 25)
}
\arguments{
\item{path}{Path to a downloaded `.xlsx` file.}

\item{n}{Number of leading rows to return per sheet.}
}
\value{
A named list (one element per sheet) of character tibbles, with the
  guessed header-row index stored in attribute \code{"header_row"}.

A named list (one element per sheet) of character tibbles.
}
\description{
Reads every sheet as plain text and returns the top rows plus a guess at the
header row. Use this to calibrate \code{\link{htp_default_colmap}} and the
\code{hotel_sheets} pattern for the real files.

Reads every worksheet as plain text and returns the leading rows, with the
guessed header-row index in attribute `"header_row"`. Useful for calibrating
the parser if the official format changes.
}
