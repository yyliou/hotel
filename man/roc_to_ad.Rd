% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twhotel.R, R/utils.R
\name{roc_to_ad}
\alias{roc_to_ad}
\title{Convert a Republic-of-China (Minguo) year to the Gregorian calendar}
\usage{
roc_to_ad(x, full = FALSE)

roc_to_ad(x, full = FALSE)
}
\arguments{
\item{x}{Character vector of ROC dates like "115-06-09" or ROC years.}

\item{full}{If TRUE return "YYYY-MM-DD"; otherwise the AD year only.}
}
\value{
Integer AD year (default) or `"YYYY-MM-DD"` character.

Integer year (default) or character date.
}
\description{
ROC year + 1911 = AD year. Accepts the dotted form used on the source site
(e.g. `"115-06-09"`) or a bare ROC year.

ROC year + 1911 = AD year. Accepts the dotted form used on the site
(e.g. "115-06-09") and returns the AD year as an integer, or the full
converted date string when `full = TRUE`.
}
\examples{
roc_to_ad("115-06-09")            # 2026
roc_to_ad("115-06-09", full = TRUE)
}
